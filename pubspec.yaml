name: quiet_zone_app
description: Sound Monitor – Quiet Zone Assistant
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:

  flutter:
    sdk: flutter

  
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  geolocator: ^11.0.0
  permission_handler: ^11.3.0
  noise_meter: ^5.0.1
  http: ^1.2.0
  shared_preferences: ^2.2.3
  flutter_local_notifications: ^17.2.2
  fl_chart: ^0.68.0
  provider: ^6.1.2
  cupertino_icons: ^1.0.6
  url_launcher: ^6.2.0

  firebase_core: ^3.15.2        # ← ADD THESE TWO
  firebase_auth: ^5.7.0        # ←
  cloud_firestore: ^5.6.12

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true